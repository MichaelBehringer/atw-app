# Basis-URL der Backend-API. Ohne diese Variable wird "/server/" benutzt:
# in Dev greift dann der Vite-Proxy aus vite.config.js, in Produktion die
# nginx-Route "location /server/". Nur setzen, wenn das Backend woanders liegt.
# VITE_API_URL=http://localhost:8080/
